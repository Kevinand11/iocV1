const state = {
    app: {
        name: "IOCMart"
    }
};

const getters = {
    appInfo: state => state.app,
};

const actions = {
};

const mutations = {
};

export default {
    state,
    getters,
    actions,
    mutations
};
